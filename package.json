{
  "name": "axon-shop",
  "private": true,
  "version": "1.0.0",
  "description": "AXON product shop and admin dashboard",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
